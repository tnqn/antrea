apiVersion: crd.antrea.io/v1alpha2
kind: TrafficControl
metadata:
  name: mirror-nginx-app-to-tap0
spec:
  appliedTo:
    podSelector:
      matchLabels:
        app: nginx
  direction: Both
  action: Mirror
  targetPort:
    ovsInternal:
      name: tap0